services:
  postgres:
    image: postgres:16-alpine
    container_name: rosecloud-iam-postgres
    environment:
      TZ: Asia/Shanghai
      PGTZ: Asia/Shanghai
      POSTGRES_DB: rosecloud_iam
      POSTGRES_USER: rosecloud
      POSTGRES_PASSWORD: rosecloud
    ports:
      - "5432:5432"
    volumes:
      - rosecloud_iam_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rosecloud -d rosecloud_iam"]
      interval: 2s
      timeout: 5s
      retries: 15

  mailpit:
    image: axllent/mailpit:v1.27
    container_name: rosecloud-iam-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  rosecloud_iam_pg:
